using System;
using System.Collections.Generic;
using System.Linq;
using System.Security.Cryptography.X509Certificates;
using System.Text;
using System.Threading.Tasks;

namespace ItlaPrimerModulo
{
    internal class Calculator
    {
        /* To create a Funtion we use public/private + the methods + Name of the Funtion + (declare type of variables) */
        public int addittion(int A, int B, int maximo)
        {
            if (A > B)
            {
                maximo = A;
            }
        
                else
            {
                maximo = B;
            }
            {

            return A + B;
            {
                public int substration(int C, int D) 

                { 
                 return C - D; 
                              
                }
            }
        }

       

    }
}
